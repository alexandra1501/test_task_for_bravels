<div class="text-center">
  <div class="col-md-6col-md-offset-3">
    <%= form_for(@resource) do |f| %> 
    <%= f.label :url %>
    <%= f.text_field :url %>
    <%= f.submit "Save resource", class: "btn btn-primary" %>
    <%= link_to 'Back', resources_path %>
    <% end %>
  </div>
</div>